<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="note-container bg-zinc-900 h-screen w-screen text-white p-8">
      <h1 class="text-3xl font-bold">Notes</h1>
      <div class="note-header">
        <h2>Add a New Note</h2>
      </div>
      <hr />
      <div class="note-body mt-1">
        <form id="note-form" action="/create" method="post">
          <label for="note-title">Title:</label>
          <input
            type="text"
            class="w-full bg-zinc-700 outline-none p-2"
            id="note-title"
            name="title"
          />
          <label for="note-content">Content:</label>
          <textarea
            class="w-full bg-zinc-700 resize-none outline-none"
            id="note-content"
            name="content"
          ></textarea>
          <button
            class="bg-orange-600 hover:bg-orange-700 p-1 rounded-md inline-block m-3"
            id="save-note"
          >
            Save Notes
          </button>
        </form>
        <hr />
        <div class="mt-6 card-container  flex gap-6 flex-wrap justify-center">
          <% if (files && files.length > 0) { %>
            <% files.forEach((file) => { %>
              <div class="card bg-zinc-700 h-fit w-72 p-2 rounded-md">
                <h1 class="text-xl"><%= file %></h1>
                <p class="hidden">
                  Lorem ipsum dolor sit amet consectetur, adipisicing elit. Ad
                  vero saepe sint culpa!
                </p>
                <div class="flex justify-between items-center">
                  <a href="/file/<%= file %>" class="text-orange-600"
                    >Read More.</a
                  >
                  <ul>
                    <a href="/rename/<%= file %>" class="text-zinc-200">Re-name</a>
                    <a href="/delete/<%= file %>" class="text-zinc-400">delete</a>
                  </ul>
                </div>
              </div>
            <% }); %>
          <% } else { %>
            <h3 class="text-zinc-400">No Notes here...</h3>
          <% } %>
        </div>
      </div>
    </div>